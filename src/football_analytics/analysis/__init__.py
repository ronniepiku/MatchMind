"""Analysis package — football metrics, models, and visualisations.

Modules:
    opponent_profile    — Pre-match opponent scouting
    player_performance  — Player metrics and radar charts
    visualisations      — Static and interactive plots
    xg_model            — Logistic regression xG model (baseline)
    xg_model_advanced   — Gradient boosting xG model (upgrade)
    similarity          — Player similarity engine
    tracking            — Tracking data integration
    possession_chains   — Possession sequence analysis
    set_pieces          — Set-piece analysis and clustering
    simulation          — Monte Carlo match simulation
    development         — Player development tracking
    spatial             — Voronoi tessellation and space control
    video_alignment     — Video timestamp sync and clip generation
"""
